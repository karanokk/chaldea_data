name: chaldea_data
description: The data layer of clean `chaldea` project.
version: 0.1.0

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  chaldea_domain:
    # path: ../chaldea_domain
    git:
      url: https://github.com/karanokk/chaldea_domain
  http: ^0.12.2
  shared_preferences: ^0.5.8
  json_annotation: ^3.0.0

dev_dependencies:
  analyzer: '0.39.14'
  build_runner: ^1.10.0
  json_serializable: ^3.3.0
  pedantic: ^1.9.0
  test: ^1.14.4
  mockito: ^4.1.1
